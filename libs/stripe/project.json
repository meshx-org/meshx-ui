{
    "name": "stripe",
    "targets": {
        "build": {
            "executor": "@nx/rollup:rollup",
            "options": {
                "main": "libs/stripe/src/index.ts",
                "format": ["esm"],
                "tsConfig": "libs/stripe/tsconfig.json",
                "outputPath": "dist/stripe",
                "generateExportsField": true
            }
        }
    }
}
