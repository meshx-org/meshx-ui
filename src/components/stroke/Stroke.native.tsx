import React from 'react'
import { ControlStrokeProps } from './Stroke.types'

function ControlStroke({ children }: ControlStrokeProps) {
    return children
}

function CircleControlStroke({ children }: ControlStrokeProps) {
    return children
}

function TextControlStroke({ children }: ControlStrokeProps) {
    return children
}

export { ControlStroke, CircleControlStroke, TextControlStroke }