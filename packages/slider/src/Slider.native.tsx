import React from 'react'
import { SliderProps } from './Slider.types'

export function Slider(props: SliderProps) {
    const { disabled = false, defaultValue = 0, value, onChange } = props

    return null
}
